//variables create as inputs for calculation
let foodCost = 79.25;  
let tax = 6.54;
let tip = 12.00;

//do our calculations for the variables abovve
let totalDue = foodCost + tax + tip;

//output the result to the console
console.log("food cost is" + foodCost + "and tax is" + tax);
console.log("The tip is" + tip.toFixed(2));
console("The total due is" + totalDue);